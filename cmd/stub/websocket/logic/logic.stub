package logic

import "github.com/gin-generator/ginctl/package/websocket"

func {{.Handler}}(_ *websocket.Request) (response *websocket.Response) {
	response = websocket.NewResponse()
	response.Code = 200
	response.Message = "success"
	return
}